import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hides the floating Next.js dev badge in the corner during development.
  devIndicators: false,
};

export default nextConfig;
